// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.tralves.arici"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
